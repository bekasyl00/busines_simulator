<!DOCTYPE html>
<html lang="kk">
<head>
  <meta charset="UTF-8">
  <title>Кіру</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #89f7fe, #66a6ff);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-box {
      background: white;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      text-align: center;
      width: 300px;
    }
    input {
      padding: 10px;
      width: 100%;
      margin-top: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 16px;
    }
    button {
      margin-top: 15px;
      width: 100%;
      background: #007bff;
      color: white;
      border: none;
      padding: 10px;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
    }
    button:hover {
      background: #0056b3;
    }
  </style>
</head>
<body>
  <div class="login-box">
    <h2>Кіру</h2>
    <p>Атыңызды енгізіңіз:</p>
    <input type="text" id="username" placeholder="Атыңыз...">
    <button onclick="startGame()">Бастау</button>
  </div>

  <script>
    function startGame() {
      const name = document.getElementById("username").value.trim();
      if (!name) {
        alert("Атыңызды енгізіңіз!");
        return;
      }
      // сохраняем данные
      localStorage.setItem("username", name);
      localStorage.setItem("capital", 50000);
      localStorage.setItem("currentDay", 1);
      localStorage.setItem("progress", JSON.stringify([true])); // первый день открыт
      // переход на игру
      window.location.href = "game.html";
    }
  </script>
</body>
</html>
